# End-to-end tests for the full stack.
# These were previously in the repo root — moved here for organisation.
# Run against a live Docker Compose stack:
#   docker compose -f deploy/docker/compose.prod.yml up -d
#   pytest tests/e2e/ -v
