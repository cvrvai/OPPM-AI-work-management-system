# API Contracts

## Primary references

- `docs/API-REFERENCE.md`
- `docs/api/`
- `gateway/`
- `services/`

## Purpose

Track verified API boundaries between the frontend, gateway, services, and external integrations.

## Working rules

- Add contract summaries only after verifying the code path or deployment surface.
- Link to request and response details where existing documents already cover them.